package com.dania.one;

public class backup_statements {
    /*switch (categoriesModel.getType()){
            case 0:
                Glide.with(context).load(categoriesModel.getUri()).thumbnail(0.2f).diskCacheStrategy(DiskCacheStrategy.ALL).into(holder.catIcon);
                break;
            case 1:
                holder.catIcon.setImageResource(R.drawable.trending_logo);
                break;
            case 2:
                holder.catIcon.setImageResource(R.drawable.nearby_logo);
                break;
            case 3:
                holder.catIcon.setImageResource(R.drawable.buddies_logo);
                break;
        }
         */


    /*
    From UniteStories2
     //ScaleLayoutManager scaleLayoutManager = new ScaleLayoutManager(getContext(), 1, ScaleLayoutManager.HORIZONTAL);
        //scaleLayoutManager.setInfinite(true);
        /*
        for transparent

    android:background="@color/Transparent">
        reaction_dialog.getWindow().setBackgroundDrawableResource(android.R.color.transparent);
        reaction_dialog.getWindow().getAttributes().windowAnimations = R.style.DialogSlide;

    <style name="DialogSlide">
        <item name="android:windowEnterAnimation">@anim/dialog_slide_up</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_slide_up</item>
    </style>
             //Glide.with(getContext()).load(Common.my_display_picture).thumbnail(0.5f).placeholder(R.drawable.user).diskCacheStrategy(DiskCacheStrategy.ALL).into(pp);


         */

}
